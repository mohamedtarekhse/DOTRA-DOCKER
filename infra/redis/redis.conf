# Redis configuration for ACUSEEK
# Persistence + bounded memory to prevent OOM

save 900 1
save 300 10
save 60 10000

appendonly yes
appendfsync everysec

maxmemory 2gb
maxmemory-policy allkeys-lru

# Only bind to container network
bind 0.0.0.0
protected-mode yes

# Health
tcp-keepalive 60
