# Mosquitto MQTT broker config for gate triggers / relay events
# Auth is REQUIRED (matching compose credentials). Disabled by default at the
# OS firewall — do not expose 1883 to the internet.

listener 1883
allow_anonymous false
password_file /mosquitto/config/mosquitto.passwd

persistence true
persistence_location /mosquitto/data/

log_dest stdout